#ifndef BASIC_OPS_H
#define BASIC_OPS_H

// Basic arithmetic operation function declarations
// Each function takes number of inputs (n) from user

int add(int n);        // Add n numbers
int subtract(int n);   // Subtract n numbers sequentially
int multiply(int n);   // Multiply n numbers
float divide(int n);   // Divide n numbers sequentially (float result)

#endif
